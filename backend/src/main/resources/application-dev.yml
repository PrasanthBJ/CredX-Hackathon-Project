spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:mysql://localhost:3306/credx?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Prasanth@18
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true
